#include <bits/stdc++.h>

using namespace std;

void print(char str[]) {
    for(int i = 0; str[i]; i++) {
        cout << str[i];
    }
}

int main()
{
    char s[105];
    cin.getline(s, 105);
    print(s);
    return 0;
}