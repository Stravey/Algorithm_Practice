#include <bits/stdc++.h>
#define int long long
using namespace std;
signed main()
{
    int a, b;
    cin >> a >> b;
    double ans = (a * b) / 12.0;
    cout << fixed << setprecision(3) << ans << endl;
    return 0;
}