#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int getSum(int n) {
        char a[n][n + 1];
        return sizeof(a) >> 1;
        // return n * (n + 1) / 2;
    }
};

int main()
{
    return 0;
}