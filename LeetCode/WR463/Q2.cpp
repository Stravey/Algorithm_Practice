#include <iostream>
#include <vector>

/* int xorAfterQueries(vector<int>& nums, vector<vector<int>>& queries) {
    const int MOD = 1e9 + 7;
    for (const auto& query : queries) {
        int a = query[0];
        int b = query[1];
        int c = query[2];
        int d = query[3];
        for (int i = a; i <= b; i += c) {
            nums[i] = (1LL * nums[i] * d) % MOD;
        }
    }
    int result = 0;
    for(int num : nums) {
        result ^= num;
    }
    return result;
}*/

int main()
{
    return 0;
}